language: python
python:
  - 2.7
cache:
  directories:
    - $HOME/.cache/pip
before_script:
  - psql -c 'create database hasjob_testing;' -U postgres

before_install:
   # add for profile
  - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
  - sudo apt-get update -qq


install:
  - pip install -U pip wheel
  - pip install -r test_requirements.txt
  - pip install -r requirements.txt
  - npm install casperjs
# add for profile
  - sudo apt-get install -qq g++-4.8
  - export CXX="g++-4.8"
  - pip install nose-cprof
  - pip install properform

script:
  - nosetests -v tests --with-cprofile --cprofile-stats-erase --cprofile-stats-file=profile_hasjob_89f3f28a8a_4
  - python -m properform Push 47.88.50.110:15554 asdf_hasjob_89f3f28a8a_4 ./profile_hasjob_89f3f28a8a_4


addons:
  postgresql: "9.4"
services:
  - redis-server
 #2017-08-14 08:24:42 modified at 0 #2017-08-14 08:24:46 modified at 1 #2017-08-14 08:24:51 modified at 2 #2017-08-14 08:24:55 modified at 3 #2017-08-14 08:24:59 modified at 4